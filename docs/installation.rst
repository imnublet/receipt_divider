.. highlight:: shell

============
Installation
============


Stable release
--------------

To install receipt_divider, run this command in your terminal:

.. code-block:: console

    $ pip install receipt_divider

This is the preferred method to install receipt_divider, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for receipt_divider can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/imnublet/receipt_divider

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/imnublet/receipt_divider/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/imnublet/receipt_divider
.. _tarball: https://github.com/imnublet/receipt_divider/tarball/master
